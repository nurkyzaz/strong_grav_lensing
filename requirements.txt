# Python environment for the strong-lensing theta_E CNN pipeline.
# Reproduces the working cluster env (conda "Stronglensing", Python 3.8.20).
# Recommended:
#   conda create -n lensfusion python=3.8 && conda activate lensfusion
#   pip install -r requirements.txt
#
# NOTE: paltas 0.2.0 is STOCK (unmodified). The multipole deflector class
# PEMDShearFourMultipole used by the GEN5 configs ships in upstream paltas 0.2.0
# (paltas/MainDeflector/simple_deflectors.py). Our contribution is the per-galaxy,
# isophote-anchored multipole *priors* in the config files, not a paltas fork.

# --- Simulation (paltas + its scientific stack) ---
paltas==0.2.0
lenstronomy==1.12.0
astropy==5.2.2
colossus==1.4.0
photutils==1.5.0
scikit-image==0.19.3
# galsim needs FFTW + Eigen; if pip fails, use conda:
#   conda install -c conda-forge galsim=2.5.3
galsim==2.5.3

# --- Core numerics ---
numpy==1.24.0
scipy==1.10.1
pandas==2.0.3
h5py==3.11.0
matplotlib==3.7.3

# --- CNN training / evaluation ---
# For GPU, install the torch build matching your CUDA (cluster used cu121):
#   pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cu121
torch==2.3.0
timm==1.0.27
scikit-learn==1.3.2
tqdm==4.64.1

# --- Data acquisition (only needed to (re)fetch source/catalog data, not to train) ---
astroquery==0.4.7
pyvo==1.6.2
requests==2.28.1
# acstools  # HST ACS utilities — used by some analysis scripts only; install if needed
